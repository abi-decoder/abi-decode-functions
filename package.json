{
  "name": "abi-decode-functions",
  "version": "0.1.3",
  "description": "Extract and list up function ids from opcodes.",
  "main": "dist/index.js",
  "scripts": {
    "clear": "rm -rf dist/",
    "lint": "tslint 'src/**/*.ts' 'test/**/*.spec.ts'",
    "lint:fix": "tslint 'src/**/*.ts' 'test/**/*.spec.ts' --fix",
    "test": "mocha --recursive --require ts-node/register \"test/**/*.ts\"",
    "build": "npm run lint && tsc",
    "compile": "tsc",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/abi-decoder/abi-decode-functions.git"
  },
  "keywords": [
    "ethereum",
    "abi",
    "solidity",
    "opcode"
  ],
  "author": "Yukishige Nakajo <yukishige.marie2013@gmail.com> (http://y-nakajo.hatenablog.com/)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/abi-decoder/abi-decode-functions/issues"
  },
  "homepage": "https://github.com/abi-decoder/abi-decode-functions#readme",
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/mocha": "^5.2.5",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "tslint-config-standard": "^7.1.0",
    "tslint-no-unused-expression-chai": "^0.1.3",
    "typescript": "^3.0.3"
  },
  "dependencies": {
    "ethereumjs-utils": "^5.2.5",
    "truffle-code-utils": "^1.1.1"
  }
}
